# Copy this file to .env and fill in your values after deploying the SAM stack.
# Get WebSocketURL from: sam deploy output or AWS Console → API Gateway → Connected Arena → Stages → prod
REACT_APP_WS_URL=wss://<api-id>.execute-api.eu-central-1.amazonaws.com/prod
